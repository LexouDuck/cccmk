#!/bin/sh -e

# shell command for initial project setup
after_create()
{
	make setup
}

# shell command to parse version number from versionfile
parse_versionfile()
{
	cat "$1" | egrep -o '[0-9]+(\.[0-9]+)+'
}
